// import { Video } from "../models/video.model.js";

// const uploadVideo = async (req, res) => {
//   try {
//     const { thumbnail, title, videoFile } = req.body;
//     const video = await Video.create({
//       thumbnail,
//       title,
//       videoFile,
//     });

//     res.status(200).send("video uploaded succesfully");
//   } catch (error) {}
// };

// export { uploadVideo };
